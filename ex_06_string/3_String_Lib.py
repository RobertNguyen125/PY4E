greet = 'Hello Bob'
zap = greet.lower() # turn string to lower case
print (zap)

#Python has a number of string functions which are in string lib
# invoke functions by appending the function to the string variable
# these functions dont change the string (because string is immutable),
# they return new string

#print(dir(greet)) to all string functions
